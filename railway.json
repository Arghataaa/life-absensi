{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "vite build && esbuild server/boot.ts --platform=node --bundle --format=esm --outdir=dist --alias:@contracts=./contracts --alias:@db=./db --alias:@=./src --banner:js=\"import { createRequire } from 'module';const require = createRequire(import.meta.url);\""
  },
  "deploy": {
    "startCommand": "NODE_ENV=production node dist/boot.js"
  }
}